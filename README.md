# BB Test project

1. Install nodejs version 14

2. Open console from the project root

3. Run the command `yarn install` or `npm install`

4. Run command `yarn start` or `npm run start`

5. Open `http://localhost:3000/` in browser
